services:
  db:
    image: postgres:16
    volumes:
      - pg_data:/var/lib/postgresql
    environment:
      POSTGRES_PASSWORD: password
      POSTGRES_DB: telegram_bot
    ports:
      - 5432:5432


volumes:
  pg_data: